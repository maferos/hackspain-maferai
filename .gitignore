.venv/
__pycache__/
*.pyc
simulation/out/
MUJOCO_LOG.TXT
.venv-autobio/
MJDATA.TXT
MJMODEL.TXT

# Detector weights, datasets and outputs written by the computer-vision scripts
/third_party/
results/
data/
# training runs: keep only the scripts, never weights, logs or plots
**/runs/**
!**/runs/**/
!**/runs/**/*.py
weights/
*.pt
*.torchscript
*.onnx
*.mp4
*.log
mobileclip*.ts

# Menagerie models fetched by simulation/scripts/fetch_menagerie.sh.
# No trailing slash: a worktree may link this to another checkout's copy rather
# than clone it again, and a trailing slash would not match the symlink.
simulation/third_party/mujoco_menagerie
