# Additional dependencies on top of simulation/requirements.txt (mujoco,
# numpy, pillow), which this backend also needs — install both into the same
# venv, e.g. simulation/.venv:
#   uv pip install --python simulation/.venv/bin/python -r simulation/requirements.txt -r view/backend/requirements.txt

fastapi>=0.115
uvicorn[standard]>=0.30
opencv-python-headless>=4.9
ultralytics>=8.3
# The LabState publisher (dashboard/bridge/labbridge) serves its websocket with:
websockets>=13
# Optional: with ANTHROPIC_API_KEY set, the formula chat reads free-form requests.
anthropic>=0.40
